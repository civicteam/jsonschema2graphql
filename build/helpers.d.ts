import { EntryPointBuilder } from './types';
/** This generates the default `Query` block of the schema. */
export declare const DEFAULT_ENTRY_POINTS: EntryPointBuilder;
export declare const err: (msg: string, propName?: import("graphql/tsutils/Maybe").default<string>) => Error;
